# Confluence Reader Configuration Example
#
# You can use environment variables instead of interactive input
# by setting these values in your shell:
#
# export CONFLUENCE_DOMAIN="yourcompany.atlassian.net"
# export CONFLUENCE_EMAIL="user@example.com"
# export CONFLUENCE_API_TOKEN="your-api-token-here"
# export CONFLUENCE_OUTPUT_DIR="./confluence-data"

# To create an API token:
# 1. Visit https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Click "Create API token"
# 3. Give it a descriptive name (e.g., "Confluence Reader")
# 4. Copy the token (you won't be able to see it again)
# 5. Store it securely - never commit it to version control

# Example usage with environment variables:
# CONFLUENCE_DOMAIN="mycompany.atlassian.net" \
# CONFLUENCE_EMAIL="user@example.com" \
# CONFLUENCE_API_TOKEN="ATATxxxxxxxxxxxxx" \
# ./confluence-reader
